export function MetricCard({ title, value, trend }: { title: string; value: string; trend?: string }) { return <article className="rounded-xl bg-white p-4 shadow-sm ring-1 ring-slate-100"><p className="text-xs uppercase tracking-wide text-slate-500">{title}</p><p className="mt-2 font-display text-2xl text-slate-900">{value}</p>{trend ? <p className="mt-1 text-xs text-mint">{trend}</p> : null}</article>; }
